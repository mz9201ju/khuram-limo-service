name: Deploy Vite Site to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout repository
        uses: actions/checkout@v4

      # 🚫 No caching — by simply removing cache field
      - name: 🧰 Setup Node.js (NO CACHE)
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 🚫 Clean install — no npm cache reused
      - name: 📦 Clean install dependencies
        run: |
          rm -rf node_modules package-lock.json
          npm install --force

      # 🚫 Clear any vite cache
      - name: 🧼 Clear Vite cache
        run: rm -rf node_modules/.vite

      - name: 🏗️ Build Vite project (fresh)
        env:
          VITE_WHATS_SECRET: ${{ secrets.VITE_WHATS_SECRET }}
          VITE_PAYPAL_CLIENT_ID: ${{ secrets.VITE_PAYPAL_CLIENT_ID }}
        run: npm run build -- --force

      - name: 🪄 Set index.html for GitHub Pages
        run: |
          cp dist/home.html dist/index.html
          cp dist/home.html dist/404.html

      - name: ⚙️ Disable Jekyll
        run: touch dist/.nojekyll

      - name: 📤 Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: 🚀 Deploy to GitHub Pages (fresh)
        id: deployment
        uses: actions/deploy-pages@v4
